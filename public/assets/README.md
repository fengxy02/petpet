# Assets

把正式 PNG 按需求放入对应子目录即可。当前版本会在图片缺失时自动使用 Phaser Graphics 生成占位贴图。

`animal-island-ui/` 中的图标、分隔线和 item 图标来自本地 `D:\skills\animal-island-ui-main.zip`（上游项目 `guokaigdg/animal-island-ui`）。许可证副本保存在 `animal-island-ui/LICENSE`。
